constants:   # 커스텀 속성
  jwt:
    issuer: green502@green.com # JWT 토큰 발행자 정보
    bearer-format: JWT
    claim-key: signedUser
    secret-key: DeaguGreenArtAcademyClass502MsaClassFightingGoGo # 비밀 레시피(절대 공개되어서는 안 된다)ㅇㅋ?
    access-token-cookie-name: access-token
    access-token-cookie-path: /
    access-token-cookie-validity-seconds: 1_296_000 # 15분
    access-token-validity-milliseconds: 1_296_000_000 # 15분
    refresh-token-cookie-name: refresh-token
    refresh-token-cookie-path: /api/user/reissue
    refresh-token-cookie-validity-seconds: 1_296_000 # 15일
    refresh-token-validity-milliseconds: 1_296_000_000 # 15일

spring:
  application:
    name: momoolggo
  config:
    import: "optional:file:.env[.properties]"
  datasource:
    url: ${DATASOURCE.DB_URL}
    username: ${DATASOURCE.USERNAME}
    password: ${DATASOURCE.PASSWORD}


mybatis:
  configuration:
    map-underscore-to-camel-case: true
  mapper-locations: mappers/*.xml

naver:
  client-id: l1ytziijx9
  client-secret: og3ooJJjUtk0Js9AHKgoNQuMCeg39M4kR390IJFY
